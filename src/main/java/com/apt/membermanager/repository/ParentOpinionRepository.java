package com.apt.membermanager.repository;

import com.apt.membermanager.entity.ParentOpinion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentOpinionRepository extends JpaRepository<ParentOpinion, Long> {
}