package com.apt.membermanager.repository;

import com.apt.membermanager.entity.DaycareGallery;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DaycareGalleryRepository extends JpaRepository<DaycareGallery, Long> {
}